//check latter is capital or not in c program....

#include <stdio.h>

int main() {
    char ch;
    printf("Enter the ch: ");
    scanf("%c", &ch);

    if(ch == 'A'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'a'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'B'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'b'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'C'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'a'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'D'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'd'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'E'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'F'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'f'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'G'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'g'){
        printf("%c is lower case!", ch);
    }
    else if(ch == 'H'){
        printf("%c is upper case!", ch);
    }
    else if(ch == 'h'){
        printf("%c is lower case!", ch);
    }


return 0;
}
