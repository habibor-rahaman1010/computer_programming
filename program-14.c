// divided  in c program...

#include <stdio.h>

int main() {
    int x, y;
    printf("Enter the value of (x and y): ");
    scanf("%d %d", &x, &y);

    int sum = x / y;

    printf("divide is: %d \n", sum);

return 0;
}

