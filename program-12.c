// subtraction in c program...

#include <stdio.h>

int main() {
    int x, y;
    printf("Enter the x and y: ");
    scanf("%d %d", &x, &y);

    int sub = x -y;
    printf("subtraction is: %d \n", sub);

return 0;
}
