// input character from user in c program....

#include <stdio.h>

int main() {
    int ch;
    printf("Enter first later of your name: ");

    ch = getchar();
    printf("The first later of your name is: %c \n", ch);

return 0;
}
